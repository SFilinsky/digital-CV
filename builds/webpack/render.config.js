/*
 * This is render build for project.
 *
 * It will produce PDF file based on contents of output folder.
 *
 * It shouldn't be used directly, only as part of `dev` or `prod` build.
 */

module.exports = {}
